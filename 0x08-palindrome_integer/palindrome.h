#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdlib.h>
#include <stdio.h>

int is_palindrome(unsigned long n);
unsigned long _reverse_number(unsigned long n, unsigned long temp);

#endif /* PALINDROME_H */
